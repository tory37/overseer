I want to make a secondary view that is toggleable from settings.  

- the current view would still exist as is.  
- when toggling, we move to this new view with a "are you sure?" confirmation, because it'll like be a bit jarring to rerender.
- the left most pane becomes a list of personas who have open sessions.  
- each session for that persona would be nessted underneath them, collapseable
- it would look a bit like your list of text threads on a phone
- we should be able to give sessions a title, and that title would show in this pane
- clicking the session would open our session view.  same view as now, but we just won't have tabs anymore in this.  every switch is managed by the left pane. 
